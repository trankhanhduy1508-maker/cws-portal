# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel CLI project link (chứa projectId/orgId, không phải secret nhưng
# không nên commit — tránh gắn nhầm checkout cục bộ vào 1 trong nhiều
# project Vercel đang tồn tại cho repo này, xem reports/deployment/)
.vercel

# Local env (chứa API key/URL thật khi có Backend)
.env
.env.local

# Python bytecode cache (sinh ra khi import cws_worker_full.py để test,
# vd reports/worker/setup_worker_runtime_test.ps1 - build artifact thuần,
# khong can commit)
__pycache__/
*.pyc

# Local Blender stress-render artifacts; keep the reproducible scene/script.
/tests/artifacts/

# Machine-specific output from the one-click Windows development setup.
CWS_DEV_SETUP_REPORT.txt

# Machine-local Track A Supervisor V1 databases.
/tools/track-a-supervisor/*.sqlite3

# Machine-local portable tools used by CWS_CODEX_LIGHT V2.
/.runtime-tools/
