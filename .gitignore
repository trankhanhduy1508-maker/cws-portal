# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel CLI project link (chứa projectId/orgId, không phải secret nhưng
# không nên commit — tránh gắn nhầm checkout cục bộ vào 1 trong nhiều
# project Vercel đang tồn tại cho repo này, xem reports/deployment/)
.vercel

# Local env (chứa API key/URL thật khi có Backend)
.env
.env.local
