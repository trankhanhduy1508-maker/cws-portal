import { RenderOrder } from './domain/render-order';export function toPublicJson(order:RenderOrder){return{id:order.id,projectName:order.projectName,profileId:order.profileId,status:order.status,stageProgress:order.stageProgress,paymentId:order.paymentId,paymentStatus:order.paymentStatus,estimate:order.estimate,createdAt:order.createdAt,downloadUrl:null,durationSec:order.durationSec,resultSizeBytes:order.resultSizeBytes,isPlaceholder:order.isPlaceholder,outputReady:order.status==='finished'&&!order.isPlaceholder};}